/*
 * !!! Only tested with the latest Chrome browser !!!
 * !!! Other browser may not work correctly       !!!
 */

//--------------------------
// Resolving values
//--------------------------

/*
 * Resolve the current cell's value, use "onvalue(theValue)"
 * Resolve the first available below cell's value, use "onvaluedown(theValue)"
 * Set the css value on the cell, use "oncss( { color: #000; font-size: 1em; } )"
 */